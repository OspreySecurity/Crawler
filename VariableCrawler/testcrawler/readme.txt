TestCrawler:
  run
  download - dowloads and parses the website
  domainHttps - tells if the base domain is https
  checkHttps - cheks all href and src attributes for https
  formatScore - formats and returns the score
  checkPassUrl - checks if password is in url
CrawlerEngine:
  main